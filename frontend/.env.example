# Địa chỉ backend FastAPI. Chỉ dùng phía máy chủ nên không cần tiền tố NEXT_PUBLIC_.
API_URL=http://127.0.0.1:8000

# Địa chỉ gốc của site, dùng cho metadataBase, sitemap.xml và robots.txt.
# Phải đổi khi triển khai thật, nếu không các URL này vẫn trỏ về localhost.
NEXT_PUBLIC_SITE_URL=http://localhost:3000
